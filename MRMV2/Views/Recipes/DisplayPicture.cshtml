
    <div id="slide" class="cycle-slideshow"
         data-cycle-fx="fadeout"
         data-cycle-pause-on-hover=true>
        @for (int i = 0; i < Model.getImages.Count; i++)

        {<img src="@Model.getImages[i].Picture_Location" />}
        <div id="progress"></div>

    </div>
    
